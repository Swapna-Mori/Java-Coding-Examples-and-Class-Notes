package functionalinterfaceexample;

/*
 * Following interface is userdefined functional interface because
 * it is marked with annotation called  @FunctionalInterface
 * SAM => Single Abstract Method
 */

public interface Addition {
	public void add();
}
